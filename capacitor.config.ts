import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.horoscope',
  appName: 'Horoscope App',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
